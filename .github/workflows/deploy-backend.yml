name: Runner Check
on:
  push:
    branches: [main, dev]
  workflow_dispatch:

jobs:
  ping:
    runs-on: [self-hosted, docker, prod]
    steps:
      - run: echo "✅ Picked up by $(hostname)"; uname -a